# コード修正完了後にできるリファクタ（重要10項目）

仕様どおり動くところまで直したあとで行う作業。ここでは **外から見た挙動は変えず、内部を人が追いやすい形にする**。機能追加や仕様変更は混ぜない。

1. **使われなくなったコードを消す**  
   修正の過程で残った未使用関数・import・分岐・古いエンドポイント、コメントアウト、デバッグ用 print を削除する。

2. **重複を1箇所にまとめる**  
   同じ変換・同じ判定・同じ API 呼び出しが複数ファイルにコピーされていれば、呼び出し元を1実装に寄せる。

3. **過剰な抽象を戻す**  
   実装が1つだけの Interface、何もしない Wrapper、「将来用」の汎用ユーティリティを、実際の呼び出しに合わせて簡略化する。

4. **名前をドメインの言葉に揃える**  
   `handleData` / `tmp` / `result2` を、何を表すかに直す。同じ概念が別名で散らばっていれば統一する。

5. **関数とファイルの大きさだけ整える**  
   1関数に取得・変換・保存が同居していれば分割する。逆に数行ずつ分かれすぎていれば用途単位に戻す。公開 API の形は維持する。

6. **ネストと分岐を読みやすくする**  
   早期 return、到達不能分岐の削除、フラグ引数で膨れた関数の分割。結果の値や副作用の順番は変えない。

7. **型を実行時の形に合わせる**  
   `any` や曖昧な Dict を具体型にする。DTO / ドメイン / DB 行の変換経路を1本にする。実行時のJSONやDBスキーマは触らない。

8. **AIが残した「説明とコードのずれ」を直す**  
   古いコメント、TODO のままの記述、スタブと本番が並んでいる箇所を、今の実装に合わせて消すか短くする。

9. **テストを実装詳細から離す**  
   内部メソッドやモックの呼び出し順に張り付いたテストを、入出力と副作用（API・画面・DB）の確認に寄せる。消したコード用のテストも消す。

10. **公開面と体裁だけ追随させる**  
    import 順、lint、フォーマット、README / OpenAPI の食い違いを直す。差分は「内部が読みやすくなった」と説明できる大きさに閉じる。
