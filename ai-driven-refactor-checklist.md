# AI駆動開発コードのリファクタ（重要10項目）

AIに書かせたコードは動くことが多い一方で、重複・過剰実装・命名のブレ・テストの薄さが残りやすい。リファクタはデザインパターンの適用より、**人が保守できる形に戻すこと**が中心になる。

1. **範囲を機能単位に限定する**  
   リポジトリ全体の一括整形はしない。入出力と副作用（API、画面、DB）を確認してから着手する。

2. **挙動を固定する回帰を先に置く**  
   ハッピーパスと代表的な失敗パスをテストまたは再現手順で残す。これがないと「動いていたもの」を壊しても気づけない。

3. **使っていないコードを消す**  
   未使用の関数・ファイル・エンドポイント、コメントアウト残骸、サンプル実装、一度も使われないフラグを削除する。

4. **重複と過剰な抽象を潰す**  
   コピー&ペーストの実装を1箇所にまとめる。実装が1つだけの Interface、何もしない Wrapper、「将来用」ユーティリティは戻すか捨てる。

5. **名前をドメインの言葉に揃える**  
   `handleData` / `processItem` を具体名にする。同じ概念が別名で散らばっていれば統一する。

6. **1箇所に詰まった責務を分ける**  
   取得・変換・判定・保存・通知が1関数に同居していないか分解する。UI に埋まった業務ルールはドメイン側へ、DB/HTTP の知識はインフラ側へ閉じる。

7. **型とデータの形を正す**  
   `any` や曖昧な Dict を具体型にする。API の実行時の形と型を合わせ、バリデーションは入口に寄せる。秘密情報・ダミーキーはリポジトリから除く。

8. **AI特有の「それっぽいコード」を正本に合わせる**  
   実在しない／古い API、プロジェクトに無いライブラリ、スタブのままの本番経路、実装内部をなぞるだけのテストを直すか消す。

9. **エラーと認可の抜けを揃える**  
   catch して握りつぶしていないか、認証・入力検証が一部経路だけ欠けていないかを見る。例外と戻り値エラーが混在していれば方針を1つにする。

10. **公開面と差分だけ整えて確認する**  
    機能追加は混ぜない。型チェック・lint・回帰を通し、差分が「何を良くしたか」説明できる大きさでコミットする。消したコードに紐づく無意味なテストも消す。
