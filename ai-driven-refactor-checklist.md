# プレイアシスト：修正完了後にできるリファクタ（10項目）

仕様どおり動くところまで直したあとで行う作業。**外から見た挙動（画面・API・バッチ・外部連携）は変えず**、人が追いやすい内部にする。機能追加や仕様変更は別差分にする。

進め方はプレイアシストの AI 駆動開発に合わせる。人が範囲と完了条件を決め、Cursor / Claude Code などにはディレクトリ単位のパッチを出させる。「リポジトリ全体を綺麗にして」は出さない。公開 API のドキュメントコメントとテストは CI で機械チェックされる前提で、リファクタ後もそこを落とさない。

1. **修正で余ったコードを消す**  
   直した機能の周りに残った未使用関数、古い分岐、デバッグログ、コメントアウト、使っていないフラグを削除する。対象は今回触ったモジュールに限定する。

2. **同じ処理のコピーを1箇所に戻す**  
   プレイアシスト内で、同じ変換・同じ判定・同じ API 呼び出しが画面とサーバ、または複数画面にコピーされていれば、呼び出しを1実装に寄せる。レスポンスの形は変えない。

3. **AIが足した過剰な抽象を戻す**  
   実装が1つだけの Interface、何もしない Wrapper、「将来の汎用化」用ユーティリティを、今の呼び出しに合わせて簡略化する。共通モジュール化は、実際に2箇所以上から使われているものだけ残す。

4. **名前をプレイアシストの言葉に揃える**  
   `handleData` / `tmp` / `result2` を、業務で使っている語（対象のプレイ、アシスト、セッション、施設、スタッフ操作など）に直す。同じ概念が別名で散らばっていれば統一する。公開 API 名は契約なので、内部名だけ先に揃える。

5. **画面・API・ドメインの置き場所だけ直す**  
   1関数に取得・変換・判定・保存が同居していれば分割する。画面に業務ルールが埋まっていればドメインへ、DB や外部サービス知識はインフラへ閉じる。エンドポイントのパスとステータスコードは維持する。

6. **分岐を読んで追える形にする**  
   早期 return、到達不能分岐の削除、フラグ引数で膨れた関数の分割。プレイの状態遷移の順番と副作用（保存、通知、外部送信）は変えない。

7. **型と入出力の形を実装に合わせる**  
   `any` や曖昧な Dict を、今のリクエスト／レスポンスの形に合わせる。各層で微妙に違う DTO を作らず変換経路を1本にする。実行時の JSON や DB スキーマは触らない。

8. **コメント・仕様メモとコードのずれを消す**  
   生成時の TODO、古いコメント、スタブと本番が並んでいる箇所を、今の実装に合わせて消すか短くする。機能仕様テンプレがある場合は、公開面の説明だけ実装に追随させる（仕様そのものは変えない）。

9. **テストを実装の内側から離す**  
   内部メソッドやモックの呼び出し順に張り付いたテストを、画面操作・API 入出力・副作用の確認に寄せる。消したコード用のテストは消す。カバレッジと公開 API コメントの CI が落ちないことだけを条件にする。

10. **リファクタ専用の差分にして通す**  
    lint、型チェック、テスト、フォーマットを開発環境のコマンドと CI で通す。PR は「修正コミット」と分け、AI レビューは可読性・重複・秘密情報の3点を見る。差分は「内部が読みやすくなった」と説明できる大きさに閉じる。
