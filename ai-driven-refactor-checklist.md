# AI駆動開発コードをリファクタするときの実際の作業一覧

AIに書かせたコードは「動く」ことが多い一方で、重複・過剰実装・命名のブレ・テストの薄さが残りやすい。リファクタはデザインパターンの適用よりも、**人が保守できる形に戻す作業**が中心になる。

---

## 0. 着手前（現状把握）

- 変更対象の境界を決める（機能単位 / モジュール単位。リポジトリ全体の一括リファクタは避ける）
- 現行の入出力・副作用を確認する（API、画面、バッチ、DB、外部サービス）
- 既存テストの有無とカバレッジの穴を確認する
- 動いていることを再現できる手順を残す（手動確認手順、または最小の回帰テスト）
- AIが生成した「意図コメント」「TODO」「仮実装」を洗い出す
- 依存関係とディレクトリ構成を図または箇条書きで把握する

---

## 1. 安全網を先に置く

- 挙動を固定する回帰テストを足す（ハッピーパス＋代表的な失敗パス）
- 既存テストがモック過多なら、境界（HTTP / DB / ファイル）のテストを優先して補強する
- スナップショットやゴールデンファイルがある場合、何を保証しているかを確認する
- リファクタ前後で比較できるコマンドを決める（テスト、lint、型チェック、主要画面の操作）

---

## 2. 削除系（いちばん効果が出る作業）

- 未使用の import / 変数 / 関数 / クラス / ファイルを削除する
- 一度も呼ばれていないエンドポイント・画面・設定を削除する
- コピー&ペーストで増えた重複実装を1箇所にまとめる
- 使っていないフラグ、環境変数、フィーチャー分岐を消す
- AIが足した過剰な抽象（Interface が実装1つだけ、Wrapper が何もしない、Factory が不要）を潰す
- 「将来使うかも」で残った汎用ユーティリティを削除または呼び出し元へ戻す
- コメントアウトされたコード、生成途中の残骸、サンプル実装を削除する
- 誤った・古くなったコメントを直すか消す（コードと食い違っている説明は害）

---

## 3. 命名・配置の揃え

- ファイル名・関数名・型名をドメインの言葉に揃える（`handleData` / `processItem` を具体名にする）
- 同じ概念が複数の名前で呼ばれていないか統一する
- 1ファイルが大きすぎる場合、責務ごとに分割する（画面 / ユースケース / インフラ）
- 逆に、細かく分かれすぎたファイルを用途単位に戻す
- ディレクトリが「種類別」（components / utils / helpers だらけ）なら、機能別へ寄せるかを判断する
- import パス、公開 API、barrel export（`index.ts`）の範囲を整理する

---

## 4. 責務の切り直し

- 1関数が「取得・変換・判定・保存・通知」を全部やっていないか分解する
- UI 層にビジネスルールが埋まっていないか、ドメイン側へ移す
- DB / HTTP クライアントの知識が entiry 全体に漏れていないか閉じる
- グローバル変数・シングルトン・モジュールスコープの隠し状態を明示的な引数またはストアに出す
- エラー処理を「全部 catch して握りつぶす」から、層ごとの方針（再throw / 変換 / ユーザー表示）に揃える
- トランザクション境界、冪等性、リトライの場所を明確にする

---

## 5. データと型

- `any` / 過剰な `unknown` / 曖昧な Map・Dict を具体型にする
- 実行時の形と型定義がずれていないか（特に API レスポンス）を直す
- マジックナンバー・文字列リテラルを定数または列挙にまとめる
- null / undefined の扱いを統一する（早期 return、Optional、デフォルト値）
- DTO・ドメインモデル・DB 行の変換箇所を1経路にする（各層で微妙に違う形を作らない）
- バリデーションを入口に寄せ、内部では信頼できる形で扱う

---

## 6. 制御フローの単純化

- 深いネストを早期 return に直す
- 同じ条件分岐のコピーを関数またはテーブル駆動にする
- 到達不能な分岐、常に true/false の条件を削除する
- 例外と戻り値エラーが混在していれば方針を1つに揃える
- 非同期の直列待ちが不要なら、依存のない処理を整理する（ただし早すぎる並列化はしない）
- フラグ引数（`isEdit`, `mode`）で挙動が分岐しすぎている関数を分ける

---

## 7. AI特有の「それっぽいコード」の除去

- 実在しない API・古い API・プロジェクトに無いライブラリの呼び出しを正本に合わせる
- README やコメントだけの実装（スタブのまま本番経路になっている箇所）を実装または削除する
- 過剰な try/catch、無意味なログ、デバッグ print を整理する
- テストが「実装の内部をなぞっているだけ」なら、仕様（入出力）に対するテストに書き直す
- 生成されたダミーデータ・ハードコード認証情報・サンプルキーを除去する
- ライセンスや出典が不明なコピペ断片があれば、自前実装に置き換える

---

## 8. 横断関心事

- ログの粒度と個人情報の出方を揃える
- 認可・認証チェックが一部の経路だけ欠けていないか足す
- 入力検証、出力エンコード、SQL / コマンド組み立てを安全な API に寄せる
- 設定値をコード埋め込みから設定・環境変数へ移す（秘密情報はリポジトリに残さない）
- タイムゾーン、ロケール、文字コードの扱いを統一する
- ページネーション、タイムアウト、キャンセル、リトライの方針を揃える

---

## 9. テストとドキュメントの追随

- リファクタで動かした公開 API のテスト名・アサーションを仕様に合わせて直す
- 消したコードに紐づくテストを削除する（落ちないように残した無意味なテストを残さない）
- モックの期待値が実装詳細に張り付いていないか緩める
- 動作確認手順、README、OpenAPI、画面仕様の食い違いを直す
- 変更理由が残る粒度でコミットを切る（「ついで」の機能追加は混ぜない）

---

## 10. 仕上げと確認

- 型チェック、lint、フォーマットを通す
- 対象機能の回帰（自動化＋主要な手動パス）を行う
- 差分を「削除が多いか」「責務が読みやすくなったか」でレビューする
- パフォーマンス悪化（N+1、不要な再レンダー、同期ブロック）が出ていないか見る
- 次に触る人が迷わない公開面（関数名・モジュール境界）だけ残し、内部は短くする

---

## 作業の進め方（実務上の順番）

1. 範囲を決める
2. 回帰の安全網を足す
3. 削除と重複排除
4. 命名とファイル配置
5. 責務とデータフロー
6. 型・エラー・横断関心事
7. テストとドキュメント更新
8. 確認して小さくコミットする

機能追加とリファクタは別コミットにする。AI に「全体を綺麗にして」と投げて一括置換すると、差分がレビュー不能になりやすい。人が範囲と完了条件を決めたうえで、生成はパッチ単位に使う。

---

## 完了の目安

- 未使用コードと重複が対象範囲から落ちている
- 名前を読めばデータの流れが追える
- 公開 API の挙動がテストまたは手順で固定されている
- 秘密情報・スタブ・誤コメントが残っていない
- 差分が「何を良くしたか」説明できる大きさになっている
